<!doctype html>
<html lang="en">
<% include ../partials/head %>

<body>
    <% include ../partials/header %>

<list>
    <% breeds.forEach(function(breed) { %>
        <li><%= breed.breed %> - <%= breed.link %></li>
    <% }); %>
  </list>

    <% include ../partials/footer %>

</body>
</html>
